PORT=3000
# Set ONE of these to enable real LLM policy extraction + recommendations.
# Leave both blank and the app still works fully via the built-in heuristic engine.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
